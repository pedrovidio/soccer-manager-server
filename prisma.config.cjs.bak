require('dotenv/config');

module.exports = {
  datasources: {
    db: {
      url: process.env.DATABASE_URL,
    },
  },
  generators: {
    client: {
      provider: 'prisma-client-js',
    },
  },
};
